# Build environment (not part of the software)
Dockerfile
docker-compose.yml
.dockerignore
scripts/

# Rust/Tauri build artifacts
src-tauri/target/
target/

# Builds are published separately
release/
*.exe

# AI agent documentation (not part of the software)
AGENTS.md

# VS Code dev container config (build environment)
.devcontainer/
